abstract class DriverRatingDuration {
  static const Duration animation = Duration(seconds: 2);
  static const Duration summaryAnimation = Duration(milliseconds: 1200);
  static const Duration alert = Duration(seconds: 2);
  static const Duration transition = Duration(milliseconds: 500);
  static const Duration changePage = Duration(milliseconds: 100);
  static const Duration connectTimeout = Duration(seconds: 30);
}
