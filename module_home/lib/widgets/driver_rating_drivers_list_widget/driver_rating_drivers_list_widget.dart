import 'package:flutter/material.dart';

class DiverRatingDiversListWidget extends StatelessWidget {
  const DiverRatingDiversListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
